// Another way

let counter = function(arr){
    return "There are " + arr.length + " elements in this array";
};
 let sum = function (a,b) {
     return a + b;
 };

 let pi = 3.142;

module.exports= {
    counter:counter,
    sum:sum,
    pi:pi
};
